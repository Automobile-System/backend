package com.TenX.Automobile.enums;

public enum NotificationType {
    SYSTEM,    // Only in-app notification (save to DB)
    EMAIL,     // Only email notification
    BOTH       // Both in-app and email
}
