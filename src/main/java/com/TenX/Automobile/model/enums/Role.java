package com.TenX.Automobile.model.enums;

/**
 * Enterprise-level user roles enum
 * Defines the hierarchy: ADMIN > MANAGER > EMPLOYEE > USER
 */
public enum Role {
    CUSTOMER,
    STAFF,
    MANAGER,
    ADMIN
}